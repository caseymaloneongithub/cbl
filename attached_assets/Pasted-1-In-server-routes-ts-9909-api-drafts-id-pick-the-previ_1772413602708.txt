1) In server/routes.ts:9909 (/api/drafts/:id/pick), the previous-picks gate uses resolved = made/skipped, plus deadline-passed only for round > 1 (lines ~9933-9946). Is this exactly the intended rule split for round 1 vs later rounds?
2) In server/routes.ts:10113 (/api/drafts/:id/commissioner-pick), I don’t see the same “draft started / previous picks resolved” checks as /pick. Should commissioner-pick obey the same sequencing rules, or is commissioner intentionally allowed to bypass them?
3) In server/routes.ts:9818 (/start-picks-now), first pick “start now” updates scheduledAt only (line ~9847), not deadlineAt. Should first-pick deadline stay at original schedule or shift to now + duration?
4) In CommissionerDraftDetail.tsx:382, “picksNotStartedYet” is active + hasTiming + no currentSlot. Could this show “Start Picks Now” in cases other than true pre-first-pick state? Is that acceptable?
5) In server/routes.ts:9859 (/skip-pick), currentOnClock is the earliest unmade+unskipped slot (lines ~9887-9891), while /timing treats some deadline-passed later-round slots as resolved. Should skip logic align with /timing “currentSlot” logic?
6) In server/storage.ts:3800, getOldestEligibleOpenSlotForUser returns oldest unmade slot only (not madeAt null), including skipped/future slots. Should eligibility live here, or is caller-level gating the intended pattern?
7) In server/routes.ts:9737 (/timing), scheduledAt is explicitly checked for overall pick #1 (line ~9770). For later picks, should scheduledAt also gate opening, or is “previous resolved” sufficient by design?
8) In routes.ts, processAutoDraft is triggered mainly by pick/skip/resume/timing calls (lines ~9215, 9800, 9898, 9996, 10178). Is event-driven auto-draft sufficient, or do we need a background scheduler so deadlines progress even when no clients are polling?
9) In routes.ts:10553 (processAutoDraft), auto-skip on deadline is implemented for round > 1 with no auto-list (lines ~10595, ~10628). Are we intentionally never auto-skipping round 1?
10) In storage.ts:4100, getTopAvailableTeamAutoDraftPick picks first unclaimed org, but does not verify that org still has available players. Should empty orgs be skipped to avoid team auto-draft stalls/errors?
11) In routes.ts completion checks (e.g., ~9993 and processAutoDraft branches), draft auto-completes only when all slots have madeAt. Should skipped-but-unmade slots block completion, or should they count as resolved?
12) In DraftBoard.tsx:196, canPick returns true for commissioner/superadmin regardless timing. Is that UI behavior intentional, given backend still enforces restrictions on /pick?
13) In CommissionerDraftDetail.tsx:698 and :743, commissionerTargetSlot/nextOpenSlot use oldest unmade slot, not “currently eligible slot.” Should commissioner tooling show “oldest unmade” or “eligible now” to match league rules?
14) In DraftBoard.tsx:239, showPreDraftBanner is true when draft is active but currentSlot is null. If currentSlot can be null mid-draft, should banner messaging be refined?
